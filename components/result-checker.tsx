'use client';
import {useState} from 'react';
export default function ResultChecker(){const [sent,setSent]=useState(false);return <main><div className="pageHero"><div className="container"><span className="eyebrow">Academic services</span><h1>Secure result checker.</h1><p>Enter your examination details to access a published result. The live lookup will be connected to the school API.</p></div></div><section><div className="formWrap"><div className="notice"><strong>Privacy first.</strong> Only published results should be returned. The backend will validate requests, rate-limit attempts and prevent student-ID enumeration.</div><form onSubmit={e=>{e.preventDefault();setSent(true)}}><label>Student / examination number</label><input required placeholder="Enter your number"/><label>Session</label><select><option>2025/2026</option><option>2024/2025</option></select><label>Term</label><select><option>First Term</option><option>Second Term</option><option>Third Term</option></select><label>PIN / token (if required)</label><input placeholder="Optional security PIN"/><button className="btn" type="submit" style={{marginTop:20,width:'100%'}}>Check published result</button></form>{sent&&<div className="notice" style={{marginTop:20}}>Demo interface submitted. Connect `NEXT_PUBLIC_API_URL` to the production result endpoint before enabling real lookups.</div>}</div></section></main>}
