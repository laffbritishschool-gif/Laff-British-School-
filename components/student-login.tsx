'use client';
import {useState} from 'react';
export default function StudentLogin(){const [ok,setOk]=useState(false);return <main><div className="pageHero"><div className="container"><span className="eyebrow">Student services</span><h1>Student portal</h1><p>Sign in to access your own school information securely.</p></div></div><section><div className="formWrap"><form onSubmit={e=>{e.preventDefault();setOk(true)}}><label>Student ID / email</label><input required autoComplete="username" placeholder="Student ID"/><label>Password</label><input required type="password" autoComplete="current-password" placeholder="Password"/><button className="btn" type="submit" style={{marginTop:20,width:'100%'}}>Sign in</button></form>{ok&&<div className="notice" style={{marginTop:20}}>Demo sign-in submitted. The production version will authenticate through the backend and redirect to the protected student dashboard.</div>}</div></section></main>}
